# SQLite version 3.x
# gem install sqlite3
# 
# Ensure the SQLite 3 gem is defined in your Gemfile
# gem 'sqlite3'

development:
    :encoding: utf8
    adapter: mysql2
    database: redjam
    username: oleg
    password: 050419902
    socket: /var/run/mysqld/mysqld.sock
    host: localhost
    pool: 5

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.

test:
    adapter: postgresql
    database: redjam
    username: redjam_test
    password: redjam_test

production:
    adapter: postgresql
    database: redjam
    username: redjam
    password: redjam
